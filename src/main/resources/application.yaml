spring:
  profiles:
    active: dev
  application:
    name: smart-city-service

server:
  port: 8083


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always